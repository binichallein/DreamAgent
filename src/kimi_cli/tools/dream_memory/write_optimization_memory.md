Write one durable inference-optimization memory to the Dream memory library.

Use this only when Dream mode is active and the session has produced a reusable lesson about model inference optimization. The memory should be specific enough to help future work on similar hardware, model families, architectures, precision choices, and inference backends.

Record successful and failed attempts. For failed attempts, set `success=false` and explain the failure in `failure_reason` and `detail_description`.

Do not store unrelated product notes, personal facts, generic coding advice, or user preferences.
