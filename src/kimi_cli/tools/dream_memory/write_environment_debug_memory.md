Write one durable environment deployment/debug memory to the Dream memory library.

Use this only when Dream mode is active and the session has produced a reusable lesson about configuring, deploying, debugging, or verifying an inference environment. Good memories include CUDA/driver/runtime mismatches, Python package conflicts, model-serving backend setup, remote GUI/STT issues, filesystem/auth/network blockers, and reproducible commands that fixed the problem.

The memory must identify the issue signature, symptoms, root cause, solution, and verification. Prefer concrete package versions, commands, and environment details over vague descriptions.

Do not store unrelated product notes, personal facts, generic coding advice, or user preferences.
